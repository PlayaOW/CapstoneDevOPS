version: "5.1"
services:
  grafana:
    image: grafana/grafana
    container_name: grafanaWeb
    restart: unless-stopped
    ports:
      - '5240:3000'
    volumes:
      - grafana-storage:/var/lib/grafana
volumes:
  grafana-storage: {}
